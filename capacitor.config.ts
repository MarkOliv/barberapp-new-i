import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "hairfashion.Barber",
  appName: "Hairfashion Barber",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
